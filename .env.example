# Copy to .env.local for local development.
# In CI these are set as GitHub Actions Secrets (see below).

# ── Production (this repo's main branch → gh-pages) ──────────────────────────
# Secrets: VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_URL=https://fryywsftyzruirmutgwp.supabase.co
VITE_SUPABASE_ANON_KEY=your_prod_anon_key_here

# ── Staging (this repo's staging branch → customer-journey-mapper-staging) ───
# Secrets: STAGING_SUPABASE_URL, STAGING_SUPABASE_ANON_KEY, ACTIONS_DEPLOY_KEY
# Staging uses a SEPARATE Supabase project for full data isolation.
# VITE_BASE and VITE_APP_ENV are set automatically by the workflow per branch.
